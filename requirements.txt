# Web UI Python SDK Integration Requirements
# For production deployment, install these Python dependencies

# Core SDK dependencies
requests>=2.31.0
python-dotenv>=1.0.0

# FastAPI backend service
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
httpx>=0.25.0

# Web UI Python SDK (installed from GitHub repository)
# git+https://github.com/Zeeeepa/web-ui-python-sdk.git

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Production server
gunicorn>=21.2.0

# Installation Instructions:
# 1. Run: cd python_backend && ./setup.sh
# 2. This will create venv, install dependencies, and clone the SDK
# 3. Start service: python zai_service.py
# 4. Test integration: python test_integration.py